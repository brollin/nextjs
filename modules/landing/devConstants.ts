/**
 * Developer UI: when `true`, the dev panel starts visible. Set to `false` to test
 * the normal experience (panel hidden until you double-click / double-tap the sky).
 */
export const DEV_SHOW_DEV_UI_INIT = false;
